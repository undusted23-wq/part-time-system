# 大学生兼职服务系统核心业务流程设计

## 1. 文档说明

本文档基于当前系统设计材料与现有实现整理系统内所有核心业务流程，重点覆盖以下来源：

1. 系统总说明与模块划分：`part-time-system/system outline.md`
2. 用例图与流程图设计：`drawio_specs/fig2_1_visitor_usecase.json`、`drawio_specs/fig2_2_student_usecase.json`、`drawio_specs/fig2_3_employer_usecase.json`、`drawio_specs/fig2_4_admin_usecase.json`、`drawio_specs/fig3_3_auth_flow.json`、`drawio_specs/fig3_4_job_apply_flow.json`、`drawio_specs/fig3_5_ai_flow.json`
3. 后端业务实现：`backend/src/main/java/com/example/backend/controller/`、`service/`、`model/`
4. 前端业务入口：`frontend/src/pages/`、`frontend/src/services/`

本文档目标不是简单罗列页面功能，而是将系统按“角色动作 -> 业务校验 -> 状态流转 -> 数据存储 -> 结果反馈”的方式拆解为可直接用于论文、设计说明书或答辩讲解的流程设计文稿。

## 2. 系统角色与业务边界

系统共包含四类业务角色：

### 2.1 游客

游客不需要登录即可访问公开信息，核心职责是浏览和检索平台内容，为后续注册转化提供入口。游客不参与写入型业务。

### 2.2 学生用户

学生是平台求职侧的核心业务主体，承担以下核心行为：

1. 注册、登录并维护个人资料
2. 管理个人简历与附件
3. 搜索、收藏、申请兼职岗位
4. 跟踪应聘进度并与企业沟通
5. 对兼职经历进行评价
6. 使用推荐与智能问答能力辅助求职

### 2.3 企业用户

企业用户是岗位供给侧主体，承担以下核心行为：

1. 注册、登录并维护企业资料
2. 创建企业档案并等待平台认证
3. 发布、编辑、上下线招聘岗位
4. 查看学生申请并推进筛选、面试、录用
5. 向求职者发送消息并记录招聘备注
6. 查看招聘统计与评价反馈

### 2.4 管理员

管理员是平台治理角色，承担以下职责：

1. 审核企业认证
2. 管理用户、岗位、申请、评价、消息等全量数据
3. 对平台运营状态进行维护和干预
4. 作为平台最高权限用户进行问题排查和内容治理

## 3. 核心业务对象与状态定义

为了描述流程，需要先统一关键业务对象及其状态。

### 3.1 用户对象 `User`

关键字段包括：

1. `username`、`password`、`email`
2. `fullName`、`phoneNumber`
3. `skills`
4. `role`
5. `isActive`

角色状态：

1. `ADMIN`
2. `EMPLOYER`
3. `STUDENT`

### 3.2 企业对象 `Company`

关键字段包括：

1. 企业名称、简介、行业、地点、官网、Logo
2. 联系邮箱、联系电话
3. 所属企业用户 `employer`
4. 认证标记 `isVerified`

企业业务状态可抽象为：

1. 待提交
2. 已提交待认证
3. 已认证
4. 已取消认证
5. 已删除

当前实现中，企业认证通过 `isVerified` 布尔值体现，未单独抽象为状态枚举。

### 3.3 岗位对象 `Job`

关键字段包括：

1. 职位标题、描述、工作类型、地点
2. 薪资与薪资周期
3. 任职要求、福利待遇、工作时长
4. 所属企业 `company`
5. 申请截止时间 `applicationDeadline`
6. 上线状态 `isActive`

岗位业务状态可抽象为：

1. 草稿
2. 待审核
3. 招聘中
4. 已下线
5. 已过期
6. 已删除

需要说明的是：

1. 从设计流程图看，岗位发布后应进入“平台审核岗位”环节
2. 从当前代码实现看，岗位上线控制主要由 `isActive` 表示，管理员可以查看、删除、上下线岗位
3. 后端尚未实现单独的“岗位审核状态字段”，因此“审核”更多体现为管理员运营治理能力，而不是严格的状态机

### 3.4 简历对象 `Resume`

关键字段包括：

1. 简历标题、摘要、技能
2. PDF 文件路径 `resumeFilePath`
3. 文件访问地址 `fileUrl`
4. 教育经历集合 `educationList`
5. 工作经历集合 `workExperienceList`
6. 是否启用 `isActive`
7. 是否默认简历 `isDefault`

简历业务状态：

1. 新建未完善
2. 已完善
3. 已上传附件
4. 已设为默认
5. 已停用
6. 已删除

### 3.5 申请对象 `JobApplication`

关键字段包括：

1. 目标岗位 `job`
2. 申请学生 `applicant`
3. 求职信 `coverLetter`
4. 简历地址 `resumeUrl`
5. 企业备注 `employerNotes`
6. 申请时间 `appliedAt`
7. 更新时间 `updatedAt`

申请状态枚举如下：

1. `PENDING`：已提交待处理
2. `REVIEWING`：企业审核中
3. `SHORTLISTED`：已入围
4. `INTERVIEW`：面试中
5. `OFFERED`：已发 Offer
6. `ACCEPTED`：学生接受录用
7. `REJECTED`：被拒绝
8. `WITHDRAWN`：学生撤回

### 3.6 收藏对象 `SavedJob`

关键字段包括：

1. 学生 `student`
2. 岗位 `job`
3. 收藏时间 `savedAt`
4. 收藏备注 `notes`

### 3.7 消息对象 `Message`

关键字段包括：

1. 发送方 `sender`
2. 接收方 `receiver`
3. 主题 `subject`
4. 正文 `content`
5. 已读标记 `isRead`
6. 关联岗位 `relatedJob`
7. 关联申请 `relatedApplication`
8. 创建时间 `createdAt`

### 3.8 评价对象 `Review`

关键字段包括：

1. 所属岗位 `job`
2. 所属企业 `company`
3. 评价学生 `student`
4. 标题、内容、评分
5. 优点、缺点、工作时段、岗位名称
6. 是否已核验 `isVerified`
7. 是否匿名 `isAnonymous`

评价业务状态可抽象为：

1. 已提交待审核
2. 已通过审核
3. 已修改待复核
4. 已删除

当前实现中，评价新建和更新后都会设置为未核验，管理员通过 `/api/reviews/{id}/verify` 完成核验。

## 4. 系统核心业务流程总览

按照业务链路，系统核心流程可以分为以下十四类：

1. 用户注册流程
2. 用户登录与鉴权流程
3. 游客公开浏览流程
4. 企业档案创建与认证流程
5. 岗位发布与岗位生命周期管理流程
6. 学生简历创建、维护、默认化与附件上传流程
7. 学生岗位搜索与推荐流程
8. 岗位收藏与取消收藏流程
9. 学生投递申请流程
10. 企业筛选申请与录用推进流程
11. 学生申请跟踪与撤回流程
12. 消息沟通流程
13. 兼职评价提交与审核流程
14. 管理员治理与运营干预流程

此外，系统还包含两个智能化扩展流程：

1. AI 推荐排序流程
2. AI 智能问答流程

以下按流程逐项展开。

## 5. 用户注册流程设计

### 5.1 流程目标

允许学生或企业用户创建系统账户，为后续登录和业务操作建立身份基础。

### 5.2 参与角色

1. 游客
2. 认证服务
3. 用户服务

### 5.3 前置条件

1. 用户未登录
2. 用户访问注册页面
3. 用户选择注册角色，当前前端支持学生和企业用户注册

### 5.4 输入信息

1. 用户名
2. 密码
3. 确认密码
4. 邮箱
5. 姓名
6. 电话
7. 角色类型

### 5.5 主流程

1. 游客进入注册页，系统展示“学生/雇主”两个身份页签。
2. 用户填写用户名、姓名、邮箱、电话、密码、确认密码。
3. 前端先执行基础校验：
   1. 两次密码必须一致
   2. 密码长度至少 6 位
4. 前端调用 `POST /api/auth/register` 提交注册请求。
5. 后端校验用户名是否已存在。
6. 后端校验邮箱是否已被占用。
7. 若校验通过，系统构造用户对象并写入以下字段：
   1. 用户名
   2. 邮箱
   3. 原始密码
   4. 姓名
   5. 电话
   6. 角色
8. 用户服务对密码进行 BCrypt 加密。
9. 系统将新用户持久化到 `users` 表。
10. 后端返回注册成功响应。
11. 前端展示“注册成功”提示，并延时跳转到对应角色登录页。

### 5.6 异常分支

1. 用户名重复：
   1. 后端返回 `Username is already taken!`
   2. 前端提示用户更换用户名
2. 邮箱重复：
   1. 后端返回 `Email is already in use!`
   2. 前端提示用户更换邮箱
3. 网络或服务异常：
   1. 前端提示“服务器错误”或“网络错误”
   2. 保留填写内容便于重试

### 5.7 输出结果

1. 新用户账号创建成功
2. 用户进入可登录状态
3. 未自动登录，需要继续走登录流程

## 6. 用户登录与鉴权流程设计

### 6.1 流程目标

完成用户身份验证、JWT 生成、角色识别与首页路由跳转。

### 6.2 参与角色

1. 已注册用户
2. 认证控制器
3. Spring Security
4. JWT 令牌服务
5. 前端路由系统

### 6.3 前置条件

1. 用户已存在
2. 用户知道自己的用户名与密码

### 6.4 主流程

1. 用户进入登录页。
2. 登录页可带 `role` 参数，用户可选择以学生、企业或管理员身份登录。
3. 用户输入用户名和密码并提交。
4. 前端调用 `POST /api/auth/login`。
5. 后端通过 `AuthenticationManager` 执行认证。
6. 若认证成功，Spring Security 将认证结果写入安全上下文。
7. JWT 服务基于认证信息生成令牌。
8. 系统查询当前用户信息，补充返回：
   1. 用户 ID
   2. 用户名
   3. 邮箱
   4. 角色
9. 前端将 `token` 写入 `localStorage`。
10. 前端将简化用户对象写入 `localStorage`。
11. 前端检查用户实际角色是否与当前登录入口角色匹配。
12. 若角色匹配，系统按角色跳转：
   1. 管理员进入 `/admin`
   2. 企业进入 `/employer`
   3. 学生进入 `/student`

### 6.5 鉴权与会话续用流程

1. 后续所有受保护接口请求都会经过 Axios 请求拦截器。
2. 拦截器从 `localStorage` 读取 `token`。
3. 若存在令牌，则在请求头中附加 `Authorization: Bearer <token>`。
4. 后端 `JwtAuthenticationFilter` 解析令牌。
5. 若令牌合法，则将用户认证信息写入上下文。
6. 控制器再结合 `@PreAuthorize` 与自定义 `SecurityService` 完成权限校验。

### 6.6 失败分支

1. 用户名或密码错误：
   1. 后端返回 401
   2. 前端提示“登录失败：用户名或密码错误”
2. 角色入口与实际角色不匹配：
   1. 登录成功但前端不给放行
   2. 页面提示当前账户类型与所选入口不一致
3. token 过期或请求未授权：
   1. Axios 响应拦截器捕获 401
   2. 清空本地 token
   3. 自动跳转 `/login`

### 6.7 输出结果

1. 用户获得受保护接口访问权
2. 用户进入相应角色工作台
3. 系统建立基于 JWT 的无状态会话

## 7. 游客公开浏览流程设计

### 7.1 流程目标

让未登录用户可以感知平台价值，浏览企业、岗位和评价信息，但不具备写入能力。

### 7.2 子流程一：游客浏览企业信息

#### 参与角色

1. 游客
2. 前端游客页面
3. 公开企业接口

#### 主流程

1. 游客访问企业浏览页面。
2. 前端调用 `GET /api/public/companies`。
3. 后端仅返回已认证企业 `isVerified = true` 的列表。
4. 页面展示企业 Logo、名称、行业、地点、官网、联系方式、简介。
5. 游客可输入关键字触发搜索。
6. 前端调用 `GET /api/public/companies/search?name=...`。
7. 后端检索企业名称或行业，并再次过滤出已认证企业。
8. 页面展示筛选结果。

#### 业务价值

1. 控制公开展示范围，只暴露经过认证的企业
2. 降低虚假企业信息直接曝光风险

### 7.3 子流程二：游客浏览兼职岗位

#### 主流程

1. 游客访问岗位浏览页面。
2. 前端调用 `GET /api/public/jobs`。
3. 后端返回所有处于活跃状态的岗位。
4. 页面展示：
   1. 岗位标题
   2. 企业名称
   3. 工作地点
   4. 截止日期
   5. 岗位类型
   6. 薪资信息
   7. 描述、要求、福利摘要
5. 游客输入关键字后调用 `GET /api/public/jobs/search?keyword=...`。
6. 后端在活跃岗位范围内执行关键字搜索并返回结果。
7. 页面引导游客“登录后申请”。

#### 控制规则

1. 仅展示 `isActive = true` 岗位
2. 游客不能申请岗位
3. 游客不能收藏岗位

### 7.4 子流程三：游客浏览评价

#### 主流程

1. 游客访问评价页面。
2. 前端调用 `GET /api/public/reviews`。
3. 后端返回评价数据列表。
4. 前端按创建时间倒序排列。
5. 页面展示评分、企业、岗位、内容、优缺点、工作时段等。
6. 若评价为匿名，则不展示学生身份。
7. 页面提示“登录或注册后可发表评价”。

#### 设计说明

按设计逻辑，游客侧应优先看到经过审核或核验的评价内容，用于增强平台公信力。当前实现公开接口直接返回评价列表，业务设计上建议将“已核验评价优先展示”作为正式版治理策略。

## 8. 企业档案创建与认证流程设计

### 8.1 流程目标

完成企业从账号创建到企业资料建档、平台认证、公开展示的完整链路。

### 8.2 参与角色

1. 企业用户
2. 管理员
3. 企业服务

### 8.3 前置条件

1. 企业用户已注册并登录
2. 企业用户进入企业中心或企业创建入口

### 8.4 主流程

1. 企业用户填写企业信息：
   1. 企业名称
   2. 企业简介
   3. 所属行业
   4. 办公地点
   5. 官网
   6. Logo
   7. 联系邮箱
   8. 联系电话
2. 前端调用 `POST /api/companies`。
3. 后端从安全上下文获取当前登录用户。
4. 后端校验当前用户角色必须是 `EMPLOYER`。
5. 系统将当前用户绑定到 `company.employer`。
6. 系统默认设置 `isVerified = false`。
7. 企业记录入库。
8. 企业进入“待认证”状态。
9. 管理员在企业管理页面查看所有企业。
10. 管理员对目标企业执行认证或取消认证操作。
11. 前端调用 `PUT /api/companies/{id}/verify?verified=true|false`。
12. 后端更新 `isVerified` 字段。
13. 认证通过后，企业可在游客端公开展示。

### 8.5 异常分支

1. 非企业用户尝试创建企业：
   1. 后端拒绝
   2. 返回无权限响应
2. 企业编辑非本人企业：
   1. 后端校验企业归属
   2. 拒绝越权修改
3. 管理员取消认证：
   1. 企业仍保留数据
   2. 但从公开展示列表中移除

### 8.6 输出结果

1. 企业信息被平台正式纳管
2. 平台可通过认证机制控制企业公开可见性
3. 企业后续可基于自己的企业档案发布岗位

## 9. 岗位发布与岗位生命周期管理流程设计

### 9.1 流程目标

实现岗位从创建、发布、展示、维护到下线的全过程管理。

### 9.2 参与角色

1. 企业用户
2. 管理员
3. 学生用户
4. 岗位服务

### 9.3 前置条件

1. 企业用户已登录
2. 企业用户至少拥有一个企业档案
3. 当前岗位所属企业必须归当前企业用户所有

### 9.4 主流程

1. 企业用户进入“发布新职位”页面。
2. 前端加载当前企业用户拥有的企业列表。
3. 企业用户选择所属公司并填写岗位信息：
   1. 职位名称
   2. 工作地点
   3. 工作类型
   4. 薪资与周期
   5. 职位描述
   6. 任职要求
   7. 福利待遇
   8. 工作时长
   9. 截止日期
4. 前端调用 `POST /api/jobs`。
5. 后端获取当前企业用户身份。
6. 后端根据 `companyId` 查询目标企业。
7. 后端校验该企业必须属于当前登录企业用户。
8. 校验通过后创建岗位对象。
9. 系统默认设置 `isActive = true`。
10. 岗位入库并可在岗位列表中展示。
11. 学生和游客后续可在公开岗位列表中看到该岗位。

### 9.5 岗位编辑流程

1. 企业用户进入岗位列表页。
2. 选择某个岗位发起编辑。
3. 前端调用 `PUT /api/jobs/{id}`。
4. 后端校验：
   1. 当前用户为该岗位所属企业所有者
   2. 或当前用户为管理员
5. 系统更新岗位属性。
6. 若岗位被移动到新企业，则还要校验当前用户是否也拥有新企业。

### 9.6 岗位上下线流程

1. 企业用户或管理员发起“招聘中/已结束”切换。
2. 前端调用 `PUT /api/jobs/{id}/status?active=true|false`。
3. 后端校验操作者权限。
4. 更新 `isActive` 字段。
5. 若岗位下线，则不再出现在游客和学生的活跃岗位检索结果中。

### 9.7 截止时间维护流程

1. 企业或管理员修改截止日期。
2. 前端调用 `PUT /api/jobs/{id}/deadline`。
3. 后端更新 `applicationDeadline`。
4. 后续学生投递时将以新的截止时间为准。

### 9.8 管理员治理流程

管理员可以：

1. 查看全量岗位
2. 按状态、类型、企业检索岗位
3. 统计每个岗位的申请数
4. 强制上下线岗位
5. 删除违规或失效岗位

### 9.9 设计与实现映射说明

1. 设计图中存在“平台审核岗位 -> 岗位上线展示”步骤
2. 当前实现未引入单独的“待审/审核通过/审核拒绝”状态字段
3. 因此在正式业务设计中，可将该环节定义为：
   1. 简化版本：发布即上线，管理员事后监管
   2. 完整版本：引入岗位审核状态机后再变为“先审后发”

## 10. 学生简历管理流程设计

### 10.1 流程目标

支持学生维护结构化简历、上传附件、设置默认简历，为岗位申请和 AI 推荐提供基础数据。

### 10.2 参与角色

1. 学生用户
2. 简历服务
3. 教育经历服务
4. 工作经历服务
5. 文件存储目录

### 10.3 子流程一：创建简历

#### 主流程

1. 学生进入简历管理模块。
2. 学生填写简历标题、摘要、技能等基础信息。
3. 前端调用 `POST /api/resumes`。
4. 后端从安全上下文中解析当前学生身份。
5. 将当前学生绑定到 `resume.student`。
6. 若该学生当前没有任何简历，则系统自动将该简历设为默认简历。
7. 简历写入数据库。

#### 业务规则

1. 只有学生可以创建简历
2. 默认简历只能在同一学生名下存在一份

### 10.4 子流程二：编辑简历

#### 主流程

1. 学生打开已有简历。
2. 前端支持统一编辑姓名、邮箱、电话、简历标题、摘要、技能等内容。
3. 前端调用 `PUT /api/resumes/{id}`。
4. 后端校验该简历归当前学生所有。
5. 系统更新简历主体内容。
6. 若请求中包含 `student` 节点，则同步更新学生基本信息和技能标签。

#### 业务意义

1. 简历编辑不仅维护求职文档，还同步维护学生画像
2. 学生技能字段会影响后续 AI 推荐结果

### 10.5 子流程三：设置默认简历

#### 主流程

1. 学生在多份简历中选择一份作为默认简历。
2. 前端调用 `PUT /api/resumes/{id}/default`。
3. 后端先查询该学生名下所有简历。
4. 系统将原默认简历全部置为非默认。
5. 再将目标简历置为默认。

#### 输出结果

1. 学生后续查看默认简历时始终能获得唯一结果
2. 系统可基于默认简历计算简历完整度和求职画像

### 10.6 子流程四：上传与下载简历附件

#### 上传流程

1. 学生选择本地 PDF 或文档文件。
2. 前端构造 `multipart/form-data` 请求。
3. 调用 `POST /api/resumes/{id}/upload`。
4. 后端校验简历归属。
5. 系统以 `resume_{id}_{原文件名}` 形式保存文件到 `uploads` 目录。
6. 系统将文件路径写入 `resume.resumeFilePath`。
7. 返回上传成功结果。

#### 下载流程

1. 学生或管理员访问 `GET /api/resumes/{id}/download`。
2. 后端根据简历记录中的文件路径加载实际文件。
3. 若文件存在，则以 PDF 响应流方式返回。
4. 前端生成本地下载动作。

### 10.7 子流程五：新增教育经历与工作经历

#### 教育经历流程

1. 学生发起“新增教育经历”。
2. 前端调用 `POST /api/resumes/{id}/education`。
3. 后端将该教育经历挂接到目标简历。
4. 通过 JPA 级联持久化写入教育表。

#### 工作经历流程

1. 学生发起“新增工作经历”。
2. 前端调用 `POST /api/resumes/{id}/work-experience`。
3. 后端将工作经历挂接到目标简历。
4. 通过级联持久化写入工作经历表。

### 10.8 简历完整度计算逻辑

学生工作台当前按以下六项计算默认简历完整度：

1. 是否存在标题
2. 是否存在摘要
3. 是否存在技能
4. 是否上传附件
5. 是否至少存在一条教育经历
6. 是否至少存在一条工作经历

完成项越多，完整度越高。

## 11. 学生岗位搜索与推荐流程设计

### 11.1 流程目标

为学生提供主动搜索与被动推荐两类求职入口。

### 11.2 子流程一：主动搜索岗位

#### 主流程

1. 学生进入“查找工作”页面。
2. 页面初次加载时调用 `GET /api/jobs/active`。
3. 系统返回所有处于招聘中的岗位。
4. 学生可以按以下条件进行组合或单项检索：
   1. 关键字
   2. 地点
   3. 岗位类型
5. 前端调用 `GET /api/jobs/search`。
6. 后端按优先级处理搜索参数：
   1. 有关键字则执行关键字搜索
   2. 有标题则按标题搜索
   3. 有地点则按地点搜索
   4. 有岗位类型则按岗位类型筛选
   5. 有最低薪资则按薪资筛选
7. 页面展示结果列表。

### 11.3 子流程二：AI 推荐岗位

#### 业务目标

根据学生技能画像和当前活跃岗位信息，对岗位进行个性化排序，提高匹配效率。

#### 主流程

1. 学生进入学生首页。
2. 前端调用 `GET /api/recommendations`。
3. 后端从当前用户身份中获得学生 ID。
4. 推荐服务读取学生 `skills`、默认简历标题、摘要、技能、教育经历和工作经历。
5. 推荐服务读取学生历史收藏岗位与投递记录，构建行为偏好画像。
6. 推荐服务读取数据库中全部活跃岗位。
7. 系统为每个岗位提取文本特征：
   1. 标题
   2. 描述
   3. 任职要求
   4. 福利待遇
   5. 企业行业与地点
8. 系统对学生画像和岗位特征做分词、归一化和权重计算。
9. 系统按“内容相似度 + 行为偏好 + 技能重合度 + 岗位新鲜度”计算综合得分。
10. 对已经投递过的岗位自动后置，避免优先推荐重复申请对象。
11. Java 服务按得分从高到低返回岗位集合。
12. 前端在学生首页将这些岗位标记为“AI 推荐”。
13. 学生可以点击刷新重新获取推荐。

#### 降级机制

1. 如果学生技能或默认简历为空，系统使用通用兼职画像作为兜底输入
2. 如果缺少收藏或投递行为数据，系统仍可仅基于内容特征完成排序
3. 推荐计算在 Java 后端本地完成，不依赖额外 Python 服务进程

#### 业务价值

1. 降低学生搜索成本
2. 将静态岗位列表改造成“排序后的机会列表”
3. 为后续个性化推荐、解释性推荐留出扩展位

## 12. 岗位收藏与取消收藏流程设计

### 12.1 流程目标

支持学生暂存目标岗位，形成待投递池。

### 12.2 参与角色

1. 学生
2. 收藏服务
3. 申请服务

### 12.3 收藏流程

1. 学生在岗位列表点击收藏按钮。
2. 前端调用 `POST /api/saved-jobs/job/{jobId}`。
3. 后端根据登录态获取当前学生。
4. 后端查询目标岗位。
5. 后端检查该岗位是否已被当前学生收藏。
6. 若未收藏，则创建 `SavedJob` 记录。
7. 如有需要，可同时写入收藏备注。
8. 收藏成功后前端更新本地收藏状态。

### 12.4 取消收藏流程

1. 学生点击取消收藏。
2. 前端调用 `DELETE /api/saved-jobs/job/{jobId}`。
3. 后端检查该岗位是否确实处于收藏状态。
4. 若存在，则删除对应收藏记录。
5. 前端将该岗位从收藏列表中移除。

### 12.5 收藏列表查询流程

1. 学生进入“收藏管理”。
2. 前端调用 `GET /api/saved-jobs`。
3. 后端返回收藏记录时，额外计算 `isApplied` 字段。
4. 该字段通过申请服务判断学生是否已对该岗位投递。
5. 前端可据此标记“已申请”或“待申请”。

## 13. 学生投递申请流程设计

### 13.1 流程目标

完成学生对岗位的正式投递，并启动岗位申请生命周期。

### 13.2 参与角色

1. 学生
2. 岗位服务
3. 申请服务
4. 企业用户

### 13.3 前置条件

1. 学生已登录
2. 目标岗位存在
3. 岗位处于招聘中
4. 岗位申请截止时间未过
5. 学生尚未对该岗位重复申请

### 13.4 主流程

1. 学生在岗位列表点击“申请”。
2. 前端构造申请参数：
   1. `jobId`
   2. 可选 `coverLetter`
   3. 可选 `resumeUrl`
3. 前端调用 `POST /api/applications`。
4. 后端解析当前登录用户，并校验其角色必须为 `STUDENT`。
5. 后端读取目标岗位。
6. 系统校验岗位截止时间：
   1. 若 `applicationDeadline < now`，禁止申请
7. 系统校验岗位是否仍为激活状态：
   1. 若 `isActive = false`，禁止申请
8. 系统校验当前学生是否已经申请过该岗位：
   1. 若已申请，禁止重复提交
9. 若以上校验全部通过，创建 `JobApplication`。
10. 系统初始化申请状态为 `PENDING`。
11. 设置申请时间和更新时间。
12. 持久化申请数据。
13. 返回“提交成功”结果。

### 13.5 异常分支

1. 截止日期已过：返回“该岗位已过申请截止时间”
2. 岗位已下线：返回“不再接受申请”
3. 重复申请：返回“已申请过该岗位”
4. 非学生身份：拒绝访问

### 13.6 业务结果

1. 学生申请记录写入系统
2. 企业端可以查看该岗位下新增申请
3. 学生端可在“我的申请”中跟踪后续状态

## 14. 企业筛选申请与录用推进流程设计

### 14.1 流程目标

实现企业从查看申请、添加备注、联系学生到推进录用的完整招聘闭环。

### 14.2 参与角色

1. 企业用户
2. 学生用户
3. 申请服务
4. 消息服务

### 14.3 前置条件

1. 企业用户已登录
2. 企业用户拥有至少一个岗位
3. 申请属于当前企业名下岗位

### 14.4 企业查看申请列表流程

1. 企业进入“申请记录”页面。
2. 前端先调用企业接口获取当前企业用户名下企业列表。
3. 再按企业列表批量查询各企业的岗位列表。
4. 再按岗位列表批量查询每个岗位的申请记录。
5. 前端将所有申请合并后展示。
6. 企业可按以下维度筛选：
   1. 申请人姓名
   2. 岗位名称
   3. 申请状态
   4. 岗位维度

### 14.5 状态推进流程

企业在申请卡片上可以执行若干状态动作，形成如下业务推进链路：

1. `PENDING` / `REVIEWING` / `SHORTLISTED` -> `INTERVIEW`
2. `PENDING` / `REVIEWING` / `SHORTLISTED` -> `REJECTED`
3. `INTERVIEW` / `OFFERED` -> `ACCEPTED`

结合完整业务设计，推荐的标准流转应为：

1. `PENDING`：学生提交完成，等待企业处理
2. `REVIEWING`：企业开始审查资料
3. `SHORTLISTED`：进入候选池
4. `INTERVIEW`：安排面试
5. `OFFERED`：发出录用意向
6. `ACCEPTED`：学生接受录用
7. `REJECTED`：流程终止
8. `WITHDRAWN`：学生主动终止

当前后端允许企业直接更新为任意枚举状态，因此正式业务设计中建议在服务层补充合法状态迁移约束。

### 14.6 添加备注流程

1. 企业查看某申请时，可填写内部备注。
2. 前端调用 `PUT /api/applications/{id}/notes`。
3. 后端校验当前企业确实是该申请所属岗位的企业方。
4. 更新 `employerNotes` 字段。
5. 学生端后续可看到雇主备注。

### 14.7 联系申请人流程

1. 企业点击“联系申请人”。
2. 前端打开消息对话框。
3. 企业填写消息内容。
4. 前端调用 `POST /api/messages`。
5. 系统写入消息：
   1. 发送方为当前企业用户
   2. 接收方为申请学生
   3. 主题默认使用岗位名称或招聘沟通主题
6. 学生进入消息中心后即可查看。

### 14.8 查看简历流程

1. 如果申请中携带 `resumeUrl`，企业可以直接打开外链简历。
2. 如果后续改造为基于默认简历投递，也可以扩展为直接关联平台简历对象。

### 14.9 结果输出

1. 企业形成标准化招聘推进链路
2. 每个申请都具备可追踪状态
3. 申请人与企业之间可沉淀沟通记录和筛选备注

## 15. 学生申请跟踪与撤回流程设计

### 15.1 流程目标

让学生在投递后可持续查看申请进度，并在合理阶段主动退出。

### 15.2 主流程

1. 学生进入“我的申请”。
2. 前端调用 `GET /api/applications/my-applications`。
3. 后端根据当前登录学生查询其全部申请。
4. 页面展示：
   1. 岗位名称
   2. 企业名称
   3. 申请日期
   4. 当前状态
   5. 求职信
   6. 企业备注
5. 学生通过状态徽标识别当前进度。

### 15.3 撤回流程

1. 对于未结束申请，学生点击“撤回申请”。
2. 前端弹出二次确认框。
3. 确认后调用 `DELETE /api/applications/{id}`。
4. 后端校验当前学生是该申请申请人。
5. 当前实现直接删除申请记录。

### 15.4 设计建议

当前实现是物理删除。若从正式业务流程设计角度，建议采用以下更稳妥的方式：

1. 将申请状态更新为 `WITHDRAWN`
2. 保留历史申请轨迹
3. 企业端仍可知道该申请曾存在但已被学生撤回

这种设计更有利于：

1. 数据审计
2. 行为分析
3. 招聘漏斗统计

## 16. 消息沟通流程设计

### 16.1 流程目标

支持学生、企业、管理员三方之间进行站内消息沟通，形成围绕招聘业务的协同通道。

### 16.2 参与角色

1. 学生
2. 企业
3. 管理员
4. 消息服务

### 16.3 子流程一：发送消息

1. 用户在消息中心或业务页面触发发送动作。
2. 前端提交：
   1. `receiverId`
   2. `subject`
   3. `content`
   4. 可选 `relatedJobId`
   5. 可选 `relatedApplicationId`
3. 后端根据登录态确定发送方。
4. 后端校验接收方存在。
5. 若指定了关联岗位，则校验岗位存在。
6. 若指定了关联申请，则校验申请存在。
7. 系统生成消息记录。
8. 新消息默认 `isRead = false`。
9. 返回已发送结果。

### 16.4 子流程二：查看收件箱与发件箱

1. 用户进入消息中心。
2. 前端调用 `GET /api/messages`。
3. 后端按当前用户分别查询：
   1. 作为接收者的消息
   2. 作为发送者的消息
4. 结果以 `received` 和 `sent` 两部分返回。

### 16.5 子流程三：查看会话

1. 用户点击某个联系人。
2. 前端调用 `GET /api/messages/conversation/{userId}`。
3. 后端查询双方互发的全部消息并按时间排序返回。
4. 页面将其组织成对话形式展示。

### 16.6 子流程四：消息已读

1. 接收方打开消息详情或执行标记已读。
2. 前端调用 `PUT /api/messages/{id}/read`。
3. 后端校验当前用户是消息接收者或管理员。
4. 将 `isRead` 置为 `true`。

### 16.7 子流程五：未读消息统计

1. 前端定时或按页面切换调用 `GET /api/messages/unread/count`。
2. 后端统计当前用户未读消息数。
3. 前端在消息入口展示提醒数字。

### 16.8 管理员消息治理流程

1. 管理员调用 `GET /api/messages/admin` 查看全量消息
2. 如发现违规或异常内容，可删除目标消息
3. 删除操作需要管理员权限或消息参与方权限

## 17. 兼职评价提交与审核流程设计

### 17.1 流程目标

建立学生对企业或岗位的反馈机制，为后续求职者提供参考，同时由平台保证内容可信度。

### 17.2 参与角色

1. 学生
2. 管理员
3. 游客/其他用户
4. 评价服务

### 17.3 学生提交评价流程

1. 学生进入“写评价”页面。
2. 填写以下信息：
   1. 标题
   2. 正文内容
   3. 评分
   4. 优点
   5. 缺点
   6. 工作时段
   7. 岗位名称
   8. 是否匿名
   9. 所属企业
   10. 可选所属岗位
3. 前端调用 `POST /api/reviews`。
4. 后端获取当前登录学生。
5. 校验企业存在。
6. 若填写岗位，则校验岗位存在。
7. 系统将 `student` 绑定为当前登录学生。
8. 系统将 `isVerified` 初始化为 `false`。
9. 保存评价记录。

### 17.4 学生修改评价流程

1. 学生在“我的评价”中打开已有评价。
2. 前端调用 `PUT /api/reviews/{id}`。
3. 后端校验当前用户是评价所有者。
4. 更新评价内容。
5. 更新后系统自动将 `isVerified` 重置为 `false`。

### 17.5 管理员审核评价流程

1. 管理员进入评价管理页面。
2. 查看待核验评价。
3. 对目标评价执行“审核通过”。
4. 前端调用 `PUT /api/reviews/{id}/verify`。
5. 后端将 `isVerified = true`。
6. 评价进入已核验状态。

### 17.6 评价展示流程

1. 用户或游客访问企业评价页。
2. 系统返回企业评价列表、平均分和总数。
3. 页面展示：
   1. 标题
   2. 内容
   3. 评分
   4. 岗位
   5. 学生信息或匿名标记
   6. 核验状态
4. 平均分用于企业口碑感知和后续排序参考。

### 17.7 业务价值

1. 形成岗位质量与企业信誉反馈闭环
2. 为求职者提供更真实的参考依据
3. 借助审核机制控制虚假评价和恶意内容

## 18. 管理员治理与运营流程设计

管理员流程不是单一业务，而是对多个业务域的治理行为，以下分模块说明。

### 18.1 用户管理流程

1. 管理员查看所有用户或按角色查看用户。
2. 可更新用户资料、角色、启用状态。
3. 可删除违规用户。
4. 可为目标用户修改密码。

### 18.2 企业管理流程

1. 查看全量企业列表
2. 按认证状态、行业、关键字筛选
3. 对企业执行认证或取消认证
4. 统计认证企业与未认证企业数量

### 18.3 岗位管理流程

1. 查看全量岗位列表
2. 联动申请表统计每个岗位申请数量
3. 按类型、状态、企业筛选岗位
4. 强制关闭岗位
5. 删除违规岗位

### 18.4 申请管理流程

1. 管理员可查看全量申请
2. 可按状态查看申请分布
3. 可查看某个岗位下的申请
4. 在必要时可直接更新申请状态或补充备注

### 18.5 评价管理流程

1. 查看全量评价
2. 核验待审核评价
3. 删除违规评价
4. 监控企业口碑与内容风险

### 18.6 消息治理流程

1. 查看全量消息记录
2. 排查异常沟通内容
3. 必要时删除消息

### 18.7 系统管理与运营分析流程

1. 通过后台页面观察岗位、企业、申请等总量变化
2. 对活跃岗位、待认证企业、待处理申请进行运营干预
3. 为平台治理、答辩展示、运营说明提供可视化支撑

## 19. AI 智能问答流程设计

### 19.1 流程目标

为学生提供类对话助手能力，回答求职、岗位、技能、简历等问题，提升系统智能化体验。

### 19.2 参与角色

1. 学生
2. 前端 AI 对话页
3. 大模型服务

### 19.3 主流程

1. 学生进入 AI 助手页面。
2. 页面默认生成一条欢迎消息。
3. 学生输入问题并提交。
4. 前端先将学生消息乐观写入对话列表。
5. 前端向 `/ai/v1/chat/completions` 发送请求。
6. 请求内容包括：
   1. 模型名
   2. 历史对话消息
   3. 当前用户问题
   4. 温度参数
7. 大模型返回回答内容。
8. 前端将回答追加到消息列表。
9. 前端以打字机效果展示最后一条 AI 回复。
10. 用户可复制 AI 回复内容。

### 19.4 异常分支

1. 网络失败
2. API Key 未配置
3. 代理配置缺失

出现异常时，页面会插入一条系统错误消息提示用户检查环境。

### 19.5 设计说明

从设计图角度，智能问答流程包括：

1. 用户提问
2. 问题构造
3. 智能问答服务
4. 结果返回
5. 对话展示

当前实现主要由前端直连模型接口完成，后续可以进一步升级为：

1. 加入岗位知识库
2. 加入简历上下文
3. 加入面试问答模板
4. 加入安全过滤和审计日志

## 20. 核心业务流程之间的衔接关系

为了体现系统的整体性，必须说明各流程之间如何首尾衔接。

### 20.1 从游客到注册用户的转化链路

1. 游客浏览企业、岗位、评价
2. 对岗位或企业产生兴趣
3. 点击登录或注册
4. 成为学生或企业用户

### 20.2 从学生建档到成功入职的主链路

1. 注册登录
2. 完善个人资料
3. 创建并完善简历
4. 获取 AI 推荐或主动搜索岗位
5. 收藏意向岗位
6. 提交岗位申请
7. 查看申请进度
8. 与企业沟通
9. 进入面试
10. 接收录用结果
11. 录用完成后可回流形成评价

### 20.3 从企业建档到招聘完成的主链路

1. 企业注册登录
2. 创建企业档案
3. 管理员认证企业
4. 企业发布岗位
5. 学生开始投递
6. 企业筛选申请并添加备注
7. 企业发起消息沟通和面试推进
8. 更新申请状态至录用或拒绝
9. 企业后续获得评价反馈和统计结果

### 20.4 平台治理闭环

1. 管理员审核企业
2. 管理员监管岗位
3. 管理员管理用户和申请
4. 管理员核验评价
5. 管理员监控消息

这条治理链路贯穿所有核心流程，是平台得以稳定运行的保障。

## 21. 权限控制设计

权限控制是系统流程可成立的前提，系统采用“JWT 认证 + 角色控制 + 资源归属校验”的三层机制。

### 21.1 第一层：是否登录

由 Spring Security 负责。除 `/api/auth/**` 和 `/api/public/**` 外，其余接口默认要求认证。

### 21.2 第二层：角色控制

通过 `@PreAuthorize` 控制，例如：

1. 只有学生可投递岗位
2. 只有企业可创建企业档案和岗位
3. 只有管理员可认证企业和核验评价

### 21.3 第三层：资源归属控制

通过 `SecurityService` 校验资源是否属于当前用户，例如：

1. 简历是否属于当前学生
2. 企业是否属于当前企业用户
3. 岗位是否属于当前企业用户
4. 申请是否属于当前学生或岗位对应企业
5. 消息是否为当前用户参与的消息

这种设计保证了系统不是“仅看角色”，而是“角色 + 所有权”双重校验。

## 22. 关键流程中的设计亮点与优化建议

### 22.1 已体现的设计亮点

1. 角色边界清晰，游客、学生、企业、管理员各有独立流程
2. 申请状态枚举较完整，适合扩展标准招聘漏斗
3. 简历采用结构化字段加附件并存，既可展示也可下载
4. AI 推荐采用可降级架构，外部服务异常不会阻断主业务
5. 权限控制不仅看角色，也校验资源归属，安全边界较清楚

### 22.2 建议优先补强的流程点

1. 岗位审核应从“管理员事后监管”升级为“显式审核状态机”
2. 申请撤回建议从物理删除改为状态流转到 `WITHDRAWN`
3. 企业端申请状态更新建议限制合法迁移路径
4. 游客评价展示建议默认只展示已核验评价
5. 消息和申请流程可以进一步做强绑定，例如在面试、Offer 阶段自动生成通知
6. AI 问答建议纳入统一后端代理与日志审计

## 23. 结论

从整体上看，大学生兼职服务系统已经形成了较完整的业务闭环，能够覆盖“信息展示 -> 用户注册 -> 企业建档 -> 岗位发布 -> 简历维护 -> 求职申请 -> 招聘沟通 -> 评价反馈 -> 平台治理”的主要链条。

若从系统设计角度概括，其最核心的价值在于三点：

1. 以学生求职与企业招聘为双中心，建立双边撮合平台
2. 以申请状态、消息沟通、评价反馈为核心，形成业务闭环
3. 以企业认证、权限控制、管理员治理为保障，形成平台可控运行机制

因此，本系统的核心业务流程设计已经具备较强的完整性、可扩展性与论文表达价值，既适合作为毕业设计系统说明，也适合作为后续迭代开发的业务蓝图。
